/* Requires the Docker Pipeline plugin */
pipeline {
    agent {label 'slave01'}
    stages {
        stage('build') {
            steps {
                sh 'docker ps'
            }
        }
    }
}
